'use client';import {useMemo,useState} from 'react';import Panel from '../ui/Panel'
export default function PhysicsSim(){const [force,setForce]=useState(20);const [mass,setMass]=useState(5);const a=useMemo(()=>force/mass,[force,mass]);return <Panel className="p-6"><div className="flex items-center justify-between"><div><div className="text-xs text-cyan-300">شبیه‌سازی فیزیک</div><h3 className="text-xl font-bold mt-1">F = ma</h3></div><div className="text-3xl font-black">{a.toFixed(1)} <span className="text-sm text-white/40">m/s²</span></div></div><div className="mt-8 h-24 relative overflow-hidden rounded-2xl bg-white/[.03]"><div className="absolute top-8 right-1/2 -translate-x-1/2 w-16 h-16 rounded-2xl bg-gradient-to-br from-violet-400 to-cyan-300 shadow-2xl transition-transform" style={{transform:`translateX(${Math.min(a*8,180)}px)`}}/></div><label className="block mt-6 text-sm">نیرو: {force} N<input aria-label="نیرو" type="range" min="1" max="50" value={force} onChange={e=>setForce(+e.target.value)} className="w-full mt-2"/></label><label className="block mt-4 text-sm">جرم: {mass} kg<input aria-label="جرم" type="range" min="1" max="20" value={mass} onChange={e=>setMass(+e.target.value)} className="w-full mt-2"/></label></Panel>}
