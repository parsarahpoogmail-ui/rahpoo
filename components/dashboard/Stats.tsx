import {Clock3,Flame,Target,TrendingUp} from 'lucide-react';import Panel from '../ui/Panel';import {stats} from '@/data/mock'
export default function Stats(){return <div className="grid grid-cols-2 xl:grid-cols-4 gap-4">{[[Clock3,'ساعت مطالعه',`${stats.studyHours} ساعت`],[Target,'مباحث تکمیل‌شده',stats.topics],[TrendingUp,'میانگین آزمون',`${stats.avg}%`],[Flame,'استریک',`${stats.streak} روز`]].map(([I,t,v]:any)=><Panel key={t} className="p-5"><div className="flex justify-between"><div><div className="text-xs text-white/45">{t}</div><div className="text-2xl font-black mt-2">{v}</div></div><I className="text-violet-300" size={22}/></div></Panel>)}</div>}
