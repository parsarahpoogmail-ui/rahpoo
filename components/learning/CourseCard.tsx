import Link from 'next/link';import Panel from '../ui/Panel';import Progress from '../ui/Progress';import type {Course} from '@/types'
export default function CourseCard({course}:{course:Course}){return <Panel className="p-5 hover:-translate-y-1 transition"><div className="flex items-center justify-between"><div className="w-12 h-12 rounded-2xl bg-white/5 grid place-items-center text-2xl">{course.icon}</div><span className="text-xs text-white/40">{course.mastery}% تسلط</span></div><h3 className="font-bold mt-4">{course.title}</h3><div className="text-xs text-white/45 mt-1">{course.chapters} فصل · آخرین مبحث: {course.last}</div><div className="mt-5"><Progress value={course.progress}/><div className="flex justify-between mt-2 text-xs text-white/45"><span>{course.progress}% پیشرفت</span><span>{course.review} مرور</span></div></div><Link href={`/lesson/${course.id}`} className="block mt-5 text-center rounded-xl bg-white/5 hover:bg-violet-500/20 py-3 text-sm">ادامه یادگیری</Link></Panel>}
