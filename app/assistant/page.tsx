'use client';import {useState} from 'react';import AppLayout from '@/components/layout';import Panel from '@/components/ui/Panel';import {Send,BrainCircuit} from 'lucide-react';export default function Page(){const [q,setQ]=useState('');const [msgs,setMsgs]=useState<string[]>([]);return <AppLayout><div className="p-6 md:p-10 max-w-4xl mx-auto"><h1 className="text-3xl font-black">همراه تحصیلی</h1><p className="text-white/45 mt-2">معلم شخصی، برنامه‌ریز، تحلیل‌گر و مرورگر هوشمند</p><Panel className="mt-8 p-5 min-h-[520px] flex flex-col"><div className="flex-1 space-y-3">{msgs.length===0?<div className="h-full grid place-items-center text-center"><BrainCircuit size={42} className="mx-auto text-violet-300"/><p className="text-white/45 mt-4">یک سؤال درسی بپرس یا از من بخواه برنامه‌ات را تحلیل کنم.</p></div>:msgs.map((m,i)=><div key={i} className="rounded-2xl bg-white/5 p-4 text-sm">{m}</div>)}</div><form onSubmit={e=>{e.preventDefault();if(q){setMsgs([...msgs,`سؤال: ${q}`,'پاسخ نمونه: سؤال دریافت شد. در نسخه متصل به AI، پاسخ مرحله‌ای تولید می‌شود.']);setQ('')}}} className="flex gap-2 mt-5"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="مثلاً قانون دوم نیوتن را توضیح بده" className="flex-1 bg-white/5 rounded-2xl px-4 outline-none"/><button className="bg-violet-500 p-4 rounded-2xl"><Send size={18}/></button></form></Panel></div></AppLayout>}
