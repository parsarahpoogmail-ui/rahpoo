import {NextResponse} from 'next/server';import {z} from 'zod';import {answerEducationalQuestion} from '@/lib/ai/provider';const schema=z.object({message:z.string().min(1).max(4000)});export async function POST(req:Request){try{const body=await req.json();const parsed=schema.parse(body);return NextResponse.json({answer:await answerEducationalQuestion(parsed.message)})}catch{return NextResponse.json({error:'درخواست نامعتبر است'},{status:400})}}
